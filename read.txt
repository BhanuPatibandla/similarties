Start the docker application and after moving to the DOCKER file location using cd in command prompt use this command

==>docker build -t similarties_app && docker run -p 8080:5000 similarties_app through localhost:8080 web application can be accessed
